const SoccerPlayer = class SoccerPlayer{
    constructor (name, age, position, club
                 ){
                    this.name = name;
                    this.age= age;
                    this.position = position;
                    this.club=club;
                 }
};